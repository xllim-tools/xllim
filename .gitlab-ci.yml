image: samidj/kernelo-builder:v1.0

stages:
    - test
    - notify
    - build
    - deploy

build:
    #only:
    #    refs:
    #      - merge_requests
    #    variables:
    #      - $CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "staging" || $CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "production"

    stage: build
    script:
        - echo "building kernel"
        - python3.7 setup.py build_ext --inplace -vvv
    artifacts:
        paths:
            - ./*.so

#services:
#    - name: docker:dind
#      entrypoint: ["env", "-u", "DOCKER_HOST"]
#      command: ["dockerd-entrypoint.sh"]
#
#variables:
#    DOCKER_HOST: tcp://docker:2375/
#    DOCKER_DRIVER: overlay2
#    DOCKER_TLS_CERTDIR: ""

#staging:
##    before_script:
##        - cp kernelo.cpython-37m-x86_64-linux-gnu.so test_env/kernelo.cpython-37m-x86_64-linux-gnu.so
##        - cp cython/my_python_objects.py test_env/my_python_objects.py
##        - cd test_env
#    stage: deploy
#    image: docker:latest
#    #image: ruby:latest
#    script:
#        - docker login -u gitlab-ci-token -p $CI_JOB_TOKEN registry.gitlab.com
#        #- docker build -t registry.gitlab.com/mistis/kernelo:v1.0 .
#        - docker login -u _ -p $HEROKU_API_KEY registry.heroku.com
#        - docker pull jentkh/armadillo
#        #- docker tag registry.gitlab.com/mistis/kernelo:v1.0 registry.heroku.com/$HEROKU_APP_STAGING/web
#        - docker tag jentkh/armadillo registry.heroku.com/$HEROKU_APP_STAGING/web
#        - docker push registry.heroku.com/$HEROKU_APP_STAGING/web
#        - docker run --rm -e HEROKU_API_KEY=$HEROKU_API_KEY wingrunr21/alpine-heroku-cli container:release web --app $HEROKU_APP_STAGING

        #- apt-get update -qy
        #- apt-get install -y ruby-dev
        #- gem install dpl
        #- echo "the project directory is - $CI_PROJECT_DIR"
        #- cat $CI_PROJECT_DIR/README.md ; echo
        #- echo "and where am I? - $PWD"
        #- dpl --provider=heroku --app=$HEROKU_APP_STAGING --api-key=$HEROKU_API_KEY
        #- ls

        #    before_script:
        #        - cd test_env
        #    stage: deploy
        #    image: ruby:latest
        #    script:
        #        - apt-get update -qy
        #        - apt-get install -y ruby-dev
        #        - gem install dpl
        #        - dpl --provider=heroku --app=$HEROKU_APP_STAGING --api-key=$HEROKU_API_KEY

        #production:
        #    before_script:
        #        - cd test_env
        #    stage: deploy
        #    image: ruby:latest
        #    script:
        #        - apt-get update -qy
        #        - apt-get install -y ruby-dev
        #        - gem install dpl
        #        - dpl --provider=heroku --app=$HEROKU_APP_PRODUCTION --api-key=$HEROKU_API_KEY


        #cppcheck:
        #    image : ubuntu:bionic
        #    stage : test
        #    before_script:
        #        - apt update
        #        - apt install -y --no-install-recommends cppcheck=1.82-1
        #        - cppcheck --version
        #    script:
        #        - cppcheck src --verbose --enable=all --language=c++

#doxygen:
#    image: alpine:3.7
#    stage : test
#    before_script:
#        - apk update
#        - apk add --no-cache -t .required_apks doxygen=1.8.13-r1 gra
#        - mkdir -p build/docs/cpp_doc
#    scripts:
#        - doxygen docs/Doxyfile
#    artifacts:
#        paths:
#            - build/docs/cpp_doc/html/
