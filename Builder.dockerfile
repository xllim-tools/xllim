FROM ubuntu:focal

MAINTAINER stan.borkowski

USER root

# RUN apt-get update && \
#     apt-get install -y --no-install-recommends gcc cmake python3-dev libatlas-base-dev libarmadillo-dev libboost-dev
RUN ls
# EXPOSE 8888

# USER 1000
	
	
